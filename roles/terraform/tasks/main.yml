---
- name: Install GPG key
  become: true
  ansible.builtin.apt_key:
    url: https://apt.releases.hashicorp.com/gpg
    state: present

- name: Install PPA
  become: true
  ansible.builtin.apt_repository:
    repo: deb https://apt.releases.hashicorp.com
    state: present

- name: Install apt package
  become: true
  apt:
    name: "terraform"
    state: present
    update_cache: "yes"
